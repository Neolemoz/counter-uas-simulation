# Cognition rollup summary

Deterministic replay cognition summaries for large corpus review — explanatory only.

- LOS-fragmented replay concentration appears most strongly in `delayed_detection_sweep` and related ridge-transition fixture variants — replay-local; derived from replay pattern tags and spatial LOS layers only
- Ambiguity-heavy sweeps share 4 spatial hotspot cell(s) in overlapping ingress windows across fixture replays — replay-local; spatial grid overlap is explanatory concentration, not sensor physics proof
- Topology-sensitive replay instability clusters in `valley_sensor_sweep`, `ridge_overlap_sweep`, `saturation_assignment_sweep` cohorts — replay-local; divergence tags reflect replay taxonomy on fixture members only
- Multiple sweep families reference shared topology keys (`corridor_defense`, `valley_ingress`) in linkage exploration — replay-local; shared keys indicate fixture lineage overlap, not operational equivalence
