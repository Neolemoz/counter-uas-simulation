# Valley ingress scenario topology pack

Portable `scenario_topology_v1` source for the valley ingress SA-R0 demo (`?demo=valley_ingress`).

Validate:

```bash
python3 scripts/evaluation/validate_scenario.py fixtures/scenarios/valley_ingress
```
