# Ridge defense scenario topology pack

Portable `scenario_topology_v1` source for the ridge defense SA-R0 demo. Fictional replay fixture only.

Validate:

```bash
python3 scripts/evaluation/validate_scenario.py fixtures/scenarios/ridge_defense
```

Pack into replay bundle (see `fixtures/sa_r0/demo_ridge_defense/README.md`).
