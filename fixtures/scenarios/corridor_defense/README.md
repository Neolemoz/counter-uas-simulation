# Corridor defense scenario topology pack (skeleton)

Minimal valid pack with narrow protected zone and `ingress_corridor` overlay. Full replay content planned for B2.

```bash
python3 scripts/evaluation/validate_scenario.py fixtures/scenarios/corridor_defense
```
